@{
    ViewData["Title"] = "Nhân viên";
    if (Context.Request.Headers["X-Requested-With"] == "XMLHttpRequest")
    {
        Layout = null;
    }
}


@model IEnumerable<RetailStore.Models.User>

<div>
    <div class="bg-white border-bottom p-3 mb-3">
        <div class="row align-items-center">
            <div class="col">
                <h2 class="mb-0">Quản lý nhân viên</h2>
            </div>
            <div class="col-auto">
                <form asp-action="Index" method="get" class="d-flex justify-content-end">
                    <div class="input-group" style="width: 300px;">
                        <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" class="form-control" placeholder="Nhập tên nhân viên...">
                        <button type="submit" class="btn btn-outline-secondary">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <form asp-action="DeleteSelected" method="post">
        @Html.AntiForgeryToken()

        <div class="mb-3 px-3">
            <a class="btn btn-outline-primary" asp-controller="Staff" asp-action="Create">
                <i class="bi bi-plus-circle"></i> Thêm
            </a>

            <button type="submit" class="btn btn-outline-danger"
                    onclick="return confirm('Bạn có chắc chắn muốn xóa các mục đã chọn?');">
                <i class="bi bi-trash"></i> Xóa mục đã chọn
            </button>
        </div>

        <div class="px-3">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th><input type="checkbox" class="form-check-input" id="selectAll"></th>
                            <th>Mã nhân viên</th>
                            <th>Tên nhân viên</th>
                            <th>Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <input type="checkbox" class="form-check-input row-checkbox"
                                               name="selectedIds" value="@item.UserId">
                                    </td>
                                    <td>@item.UserId</td>
                                    <td>@item.FullName</td>
                                    <td>
                                        <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.UserId">
                                            <i class="bi bi-pencil"></i> Sửa
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="4" class="text-center">Không có dữ liệu.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </form>
</div>
<style>
    .nav-link {
        border-left: 3px solid transparent;
    }

        .nav-link.active {
            border-left-color: #0d6efd;
        }

        .nav-link:hover {
            background-color: rgba(0, 0, 0, 0.05);
        }

    .table th {
        font-weight: 600;
        white-space: nowrap;
    }

    .table td {
        vertical-align: middle;
    }
</style>
@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const selectAllCheckbox = document.getElementById('selectAll');
            const rowCheckboxes = document.querySelectorAll('.row-checkbox');

            selectAllCheckbox.addEventListener('change', function () {
                rowCheckboxes.forEach(checkbox => {
                    checkbox.checked = selectAllCheckbox.checked;
                });
            });
        });
    </script>
}